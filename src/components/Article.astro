---
import Base from '../layouts/Base.astro';

const { post } = Astro.props
//console.log(post);
---

 
<div>
 
  <img src={post.fimg_url} width="200" />
 
  <h2>{post.title.rendered}</h2>
  <a href={`/` + post.slug}>Read more</a> 
  Hells Bells
</div>
 <br/>
